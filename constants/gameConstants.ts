// Game-wide constants

// Threshold for determining if a word is spelled correctly in TRACE mode
// If accuracy score is greater than this value, the spelling is considered correct
export const CORRECT_SPELLING_THRESHOLD = 30;

// Animation timing constants
export const ROUND_RESULT_AUTO_ADVANCE_DELAY = 2000; // 2 seconds
export const FEEDBACK_DISPLAY_TIME = 1200; // 1.2 seconds - feedback toast display time
export const QUIZ_MODAL_DISPLAY_TIME = 3000; // 3 seconds - quiz modal (correct/wrong color) retention time
export const RESULT_DISPLAY_TIME = 3200; // Quiz result (score increase) modal display time
export const RESULT_MODAL_DISPLAY_TIME = 3200; // Round result display time
export const LOADING_SCREEN_TIME = 1500; // Loading screen duration

// Timer constants
export const TRACING_TIME_SECONDS = 20;
export const QUIZ_TIME_SECONDS = 15;
